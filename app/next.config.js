/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'http',
        hostname: 'localhost',
      },
    ],
    unoptimized: true,
  },
  serverExternalPackages: ['pdf-parse', 'canvas'],
}

module.exports = nextConfig
